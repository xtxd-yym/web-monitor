# 云监控系统接入与使用完整指南

> **文档状态**：V2.0 (适用 2026 最新版)
> **受众**：前端开发工程师、系统运维人员、业务线负责人

本文档详细介绍了如何将您的前端项目/组件接入云监控系统，以及如何使用控制台查看日志、配置告警和上传 SourceMap。

---

## 一、 SDK 接入指南

云监控 SDK 采用无侵入式设计，仅需一次引入即可自动收集并上报 5 大类前端错误及用户行为轨迹。

### 1. 引入方式

在 HTML `<head>` 或 `<body>` 最前部引入 SDK 文件（推荐使用 CDN 地址或拷贝至项目静态资源目录）：

```html
<script src="https://your-cdn-path/monitor.min.js"></script>
```

### 2. 初始化配置

在 SDK 引入后，实例化 `DynamicWebMonitor` 并调用 `init()` 方法：

```javascript
const monitor = new DynamicWebMonitor({
    // 1. 基础配置
    project: 'your-project-name',     // 必填：项目名称，需与后台配置一致
    env: 'production',                // 选填：运行环境，如 production / development
    
    // 2. 采集与性能配置
    autoReportThreshold: 1,           // 选填：本地累计错误达到几条后触发上报（默认 1）
    deduplicationWindow: 300000,      // 选填：去重时间窗口（毫秒），默认 5 分钟。同时间段内同指纹错误不上报
    samplingRates: {                  // 选填：各类型错误采样率 (0.0 ~ 1.0)
        'resource': 0.5,              // 示例：资源加载错误只采集 50%
        'network': 1.0,               // 示例：网络请求错误 100% 采集
        'javascript': 1.0,
        'promise': 1.0
    },

    // 3. 动态配置与多租户标识
    configUrl: 'http://b2b-openplat-test-monitor.myhexin.com/api/config', // 必填：后端拉取动态配置的地址
    apiParams: {
        appKey: 'your_appkey',        // 必填：租户/业务线唯一标识（需在管理后台生成）
        customer_name: '客户名称',     // 选填：便于在后台按客户筛选
        service_name: '组件名称'       // 选填：具体的业务组件名（如 龙虎榜2.0）
    },

    // 4. 自定义上报逻辑 (解耦上报方式，防止跨域拦截)
    monitorReport: async (data) => {
        try {
            // 格式化时间戳
            const formattedList = data.list.map(item => ({
                ...item,
                timestamp: new Date(item.timestamp || Date.now()).toLocaleString()
            }));

            // 推荐使用 fetch 配合 keepalive: true，保证页面关闭时也能发出请求
            const response = await fetch('http://b2b-openplat-test-monitor.myhexin.com/api/errors/report', {
                method: 'POST',
                keepalive: true, 
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                    ...data,
                    list: formattedList,
                    project: 'your-project-name',
                    env: 'production',
                    sdkVersion: '1.0.0'
                })
            });
            return await response.json();
        } catch (e) {
            // 注意：必须 catch 异常，防止监控自身的报错导致死循环
            console.error('上报失败', e);
            return { success: false };
        }
    },

    // 5. 本地钩子 (可选)
    onCaptureError: (error) => {
        // 当发生错误时的回调，可用于本地调试输出
        // console.log('SDK 捕获错误:', error);
    }
});

// 启动监听
monitor.init();
```

### 3. 自定义错误上报

对于一些被 `try...catch` 吞掉的错误，或者业务逻辑错误（例如接口返回状态码 200，但业务 code 提示失败），可以使用自定义上报 API：

```javascript
if (monitor && monitor.captureError) {
    monitor.captureError({
        type: 'custom',                       // 类型标识为 custom
        message: '用户支付失败：余额不足',       // 报错摘要
        timestamp: Date.now()                 // 发生时间
    });
}
```

---

## 二、 可视化管理控制台使用指南

控制台是研发排查问题、配置告警的核心枢纽。请使用管理员下发的账密（默认为 `admin/admin`）登录。

### 1. AppKey 管理 (多租户隔离)
接入 SDK 的第一步，是为您的业务组件或客户申请一个 `AppKey`。
* **位置**：左侧导航栏 -> `AppKey管理`
* **操作**：点击新增，填入业务名称，系统将自动生成一段唯一的 AppKey（如 `6507AA140000`）。
* **用途**：SDK 初始化时必传；上传 SourceMap 时必须通过 AppKey 进行目录隔离。

### 2. SourceMap 管理 (解混淆必备)
生产环境代码经过压缩后，报错信息通常为 `a.b is not a function`。需上传 `.map` 文件供系统还原。
* **位置**：左侧导航栏 -> `SourceMap管理`
* **操作**：
  1. 选择对应的 `AppKey`。
  2. 将 Webpack / Vite 打包产生的 `.js.map` 文件打包或单文件上传。
* **效果**：上传后，系统接收到该 AppKey 的错误日志时，会自动将混淆行号转换为业务真实源码的文件、行号和上下文。

### 3. 配置管理 (SDK 远程下发控制)
无需重新发版，即可在云端热更新 SDK 的行为。
* **位置**：左侧导航栏 -> `配置管理`
* **操作**：为特定的 AppKey + Project 创建配置。
* **可用控制项**：
  * **全局启停**：一键关闭该组件的监控采集。
  * **采样率调节**：遇到非致命错误（如静态图片大面积404）刷屏时，可将 `resource` 的采样率调至 `0.1` 甚至 `0` 紧急熔断。
  * **行为追踪**：控制是否开启用户轨迹（Breadcrumbs）采集。

### 4. 告警配置 (数据管理 & 实例监控)
系统采用“指标 + 实例”的方式配置告警。
* **指标类型**（系统内置）：如 `javascript`、`promise`、`resource` 等。
* **操作步骤**：
  1. 进入 `数据管理`。
  2. 点击**新建告警实例**。
  3. 配置规则：
     * **阈值与时间窗口**：例如 `3600` 秒内发生 `1` 次即告警。
     * **静默期**：例如 `600` 秒（触发告警后的 10 分钟内，相同告警不再重复发送）。
     * **通知邮箱**：填入研发负责人的邮箱。
     * **AppKey 绑定**：填入规则生效的特定 AppKey。

### 5. 错误日志与 Dashboard
* **Dashboard**：提供全局视角，查看昨日与今日的错误总数、折线图趋势、不同类型错误（白屏、JS、网络）的饼图分布，以及 **Top 10 频繁错误**排行榜。
* **错误日志详情**：
  * 支持按照环境、错误类型、客户名称、AppKey 进行多维交叉筛选。
  * 点击“详情”即可查看 **SourceMap 解析后的真实源码堆栈**。
  * **用户行为轨迹（Breadcrumbs）**：在详情右侧的时间线中，可清晰看到报错前 1-2 分钟内，用户点击了什么 DOM 元素、发起了哪些 AJAX 请求、页面是否发生了滚动或路由跳转，帮助 100% 还原案发现场。
