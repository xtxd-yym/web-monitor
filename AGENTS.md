# 云监控项目执行约束

## 运行环境与发布责任

- 云监控当前运行在正式环境；不要把代码完成、本地检查或 Mock 自测表述为已部署、已上线或生产验证通过。
- 生产部署、页面验收、真实邮件/Vanish 消息验收由用户执行。AI 只交付代码、配置说明、数据库命令和验证清单。
- 不在本地启动前端或后端，不为本地运行安装、补齐或升级依赖。默认只做静态语法检查和不会产生外部副作用的简单自测。
- 禁止在本地自测中调用真实 SMTP、扶摇或 Vanish；外部写操作必须使用 Mock。

## MySQL 变更

- 生产 MySQL 由人工管理，应用账号没有建表或改表权限。代码中不得执行 `CREATE TABLE`、`ALTER TABLE`、`DROP TABLE` 或自动迁移。
- 每次涉及 MySQL 表结构变化，直接在交付回复中给出当前场景所需的最小 DDL 命令；除非用户明确要求，不要额外创建迁移 SQL 文件。
- 已知表存在时只提供必要的 `ALTER TABLE`，不要再提供建表、探测或迁移框架等无关命令。
- SQL 由用户提交数据库工单或人工执行。AI 不连接、不修改正式数据库。
- `后端服务/src/db/schema_mysql.sql` 仅是结构参考文件，不代表应用会或可以自动执行。

## 凭证与正式验收

- `VANISH_AK`、数据库密码、SMTP 密码等只通过 b2bpass Secret/环境变量注入，不得写入代码、SQL、Markdown、日志或前端状态。
- Vanish 正式验证必须由用户部署后，在 b2bpass 正式容器内完成 DNS/TLS/网络检查，并由真实收件账号人工确认消息内容。
