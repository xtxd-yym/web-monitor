# 云监控系统 - 2026下半年迭代与演进追踪文档

> **文档说明**：本文档基于项目前期（2026年上半年）的POC、内部分享以及在「样板间」正式上线平稳运行3个月的实际情况编写，旨在梳理当前系统全貌、总结历史技术沉淀，并为2026下半年的系统重拾和深度迭代提供全局追踪指引。
> **更新时间**：2026-08-20

> **最新进展**：TD-01、TD-02、UI-01~UI-05、Ask AI 与 AI 巡检日报均已上线；日报邮件、Vanish Markdown 文本、实时规则告警 Vanish 触发及每日 09:00 自动日报连续稳定性均已完成生产验收。SDK 1.0.1 配置语义修复、精准 URL 过滤和共享 Loader/版本化发布能力已完成生产发布，并于 2026-08-19 在龙虎榜2.0完成 Loader、manifest、实际运行版本和配置拉取验证，本次试点里程碑关闭。2026-08-20 真实网关事故验证了 SDK 异常发现能力，同时暴露 Ask AI 在浏览器证据不足时对 DNS/404 过度定性的风险，已新增 P1 诊断可信度增强待办。管理端统一请求层已于 2026-08-17 迁移至原生 Fetch 并移除 Axios 直接依赖，尚未部署生产。

---

## 一、 项目当前所处阶段与回顾

经过上半年的开发和内部分享，本系统已顺利度过 POC（概念验证）阶段，并成功部署至公司正式生产环境（B2B Pass平台），作为内部「样板间」组件监控工具，持续收集了 3 个月的真实生产数据。

**阶段性成果：**
1. **得到认可**：内部分享顺利，架构设计与零侵入接入方案得到领导与团队认可。
2. **平稳运行**：在样板间中高频使用的 7 个云组件上完成了集成，持续稳定上报真实用户的报错与行为数据。
3. **闭环达成**：实现了从「前端错误捕获 -> 动态去重上报 -> 后端分析 -> 邮件实时告警 -> 可视化面板排查」的完整监控闭环。

---

## 二、 系统架构与模块梳理

项目代码库主要由四大核心模块构成（所有模块代码均已备份且功能完善）：

| 模块名称 | 所在目录 | 核心技术栈 | 当前已实现核心功能 |
| --- | --- | --- | --- |
| **监控 SDK** | `监控sdk源码/` | 原生 JS + Webpack | 5大错误自动捕获、本地防抖去重、用户行为面包屑采集、动态配置拉取（支持热更新/灰度）、采样率控制。 |
| **后端服务** | `后端服务/` | Node.js + Express | API 服务、ProxySQL 强一致性读、告警规则匹配、基于 Appkey 的 SourceMap 动态还原、JWT 登录鉴权、邮件异步发送。 |
| **可视化控制台** | `告警信息可视化网页/` | Vue 3 + Element Plus | Dashboard 看板、错误列表（含面包屑时间线）、Appkey 管理、SourceMap 管理、动态配置下发、告警规则配置。 |
| **前端演示页面** | `前端演示页面/` | HTML + Native JS | 集成 SDK 的全功能测试靶场（支持模拟各类 JS 错误、网络 CORS、白屏等场景）。 |

---

## 三、 已上线核心特性清单（Baseline）

在重拾项目前，确认当前系统已具备的稳定能力：

1. **零侵入自动采集**：业务线只需引入 `monitor.min.js` 并配置 `Appkey`，即可自动捕获 JS运行时异常、Promise 未捕获拒绝、资源加载失败、接口请求错误及页面白屏。
2. **多租户与动态配置能力**：
   * 支持多 `Appkey` 及 `Customer_name` 维度的数据隔离。
   * 支持通过控制台热更新 SDK 的上报开关和采样率（`samplingRates`）。
3. **全链路排查能力**：
   * **用户行为面包屑**：还原错误发生前的点击、滚动、路由跳转及网络请求轨迹。
   * **SourceMap 精准映射**：后端按 `Appkey` 隔离存放 `.map` 文件，自动还原压缩代码的真实报错行数与源码上下文。
4. **告警降噪与通知**：
   * 基于**错误指纹 + 时间窗口**的客户端/服务端双重去重。
   * 细粒度的告警规则引擎（阈值、时间窗口、静默期），支持公司 SMTP 邮件，并已接入 Vanish 文本消息渠道。
5. **系统安全性**：
   * 可视化控制台已接入完整的登录鉴权机制（JWT + HttpOnly Cookie），拦截越权访问。
6. **AI 辅助诊断与日报**：
   * Ask AI 已上线，可在错误详情中生成根因分析与修复建议；当前结论代表辅助排查建议，不代表已通过诊断准确性验收。
   * AI 巡检日报已上线，正式邮件与 Vanish Markdown 文本消息已实收确认。

---

## 四、 历史技术债与隐患追踪（高优修复项）

根据上半年积累的运行经验和历史代码 Review 记录，系统目前存在以下技术债，需在下半年扩大接入规模前优先解决：

### 🔴 TD-01：SDK 上报 Payload 无截断保护（静默丢失风险）
* **现状**：SDK 使用 `fetch keepalive: true` 上报，浏览器对 `keepalive` 请求有 64KB 的严格大小限制。当错误堆栈过长或包含大量面包屑数据时，超出限制会导致浏览器**静默丢弃**请求，且 SDK 内部 `catch` 未做降级处理，导致最复杂的关键错误丢失。
* **规划方案**：在发送前检查序列化后的 JSON 大小，若超过安全阈值（如 50KB），优先截断面包屑数组长度，其次截断超长堆栈，确保核心错误指纹和概要信息能够成功上报。

### 🔴 TD-02：SourceMap 解析同步阻塞 Event Loop
* **现状**：当前后端在接收到上报后，直接在请求处理生命周期内执行 `await sourcemapService.parseStack()`。读取数 MB 的 `.map` 文件并在单线程下做计算，在并发稍高时会导致后端响应严重延迟。
* **规划方案**：
  * **方案 A（推荐）**：改为**读时解析**（Lazy Load）。上报时仅落库原始混淆堆栈，当用户在后台点击「查看详情」时，再按需去解析并返回真实堆栈。
  * **方案 B**：引入消息队列（如 BullMQ），上报后将解析任务抛入后台 Worker 异步处理，处理完再更新至数据库。

### 🟡 TD-03：关系型数据库存储容量与时序查询观察项
* **2026-08-12 核验结果**：当前两张主要数据表合计约 **1.97MB**，完全没有容量压力；现阶段不需要清表、拆库或引入 ClickHouse。
* **处理原则**：继续使用 MySQL，并定期观察表容量、日新增量、慢查询和索引命中情况。只有真实数据规模或查询性能达到明确阈值后，再评估归档、索引优化或 ClickHouse，避免为尚未出现的问题提前扩容。

### 🟡 TD-04：管理端 Axios 依赖安全收敛
* **2026-08-17 处理结果**：统一请求层已迁移至浏览器原生 Fetch，删除 `axios` 直接依赖，现有页面的 `request.get/post` 调用保持不变。
* **兼容范围**：保留查询参数、JSON、HttpOnly Cookie、默认/自定义超时、FormData 上传、HTTP 错误结构、401 登录失效跳转与业务错误提示。
* **验证边界**：请求层 Mock 单测 5/5 与相关 JS 语法检查通过；本地未启动服务或安装依赖。当前为代码完成，仍需部署后人工验收登录、列表查询、数据操作、SourceMap 上传和失效会话跳转。

---

## 五、 2026 下半年迭代规划（Roadmap）

结合当前成熟度及业务发展诉求，下半年的重拾与迭代计划分为三个阶段：

### Phase 1：稳固基建，清偿债务（预计 1-2 周）
* [x] **修复核心缺陷**：TD-01（Payload 截断）与 TD-02（SourceMap 按需解析）已上线验证。
* [x] **数据库容量核验**：两张主要数据表合计约 1.97MB，确认当前无容量压力，不执行清表、加库或 ClickHouse 改造；TD-03 转为持续观察项。
* [ ] **管理端移除 Axios 后的生产验收**：TD-04 已完成编码和 5/5 Mock 验证；待构建部署管理端镜像并完成登录、查询、数据操作、SourceMap 上传及 401 跳转验收。
* [x] **SDK 1.0.1 生产发布与试点验收**：包含 `/api/monitor-sdk` HTTPS 网关别名的后端镜像已部署；龙虎榜2.0已确认 manifest 指向 1.0.1、浏览器实际运行 SDK 1.0.1 且配置拉取成功。其他存量组件后续随各自正常发版滚动迁移 Loader，不阻塞本里程碑关闭。
* [ ] **时区问题验证**：确认之前修复的告警邮件触发时间（UTC+8 偏移问题）在线上是否已完全正常。
* [ ] **下次 SDK 发版清理版本与时间兼容逻辑**：业务组件错误上报使用 Loader 返回的动态 `version`；SDK 配置请求中的历史 `version: '1.0.0'` 当前未被后端配置接口使用，不影响 1.0.1 功能，本次不阻塞发布。下次发版改为构建注入的 `SDK_VERSION`。Loader 迁移期间保留业务组件现有 `toLocaleString()` 时间转换，后续单独完成毫秒时间戳入库及 Asia/Shanghai 展示链路验证后再移除。

### Phase 2：业务赋能，拓展性能监控（预计 3-4 周）
* [ ] **P1 Ask AI 诊断可信度增强**：以 2026-08-20 datacenter 网关事故作为首个真实回归样本，修正 `status=0` 和资源加载失败的基础分类边界；AI 输出必须区分已确认事实、候选原因、置信度、缺失证据、验证步骤和建议处理方。按客户、组件、域名、错误指纹和时间窗口聚合同类异常，批量同时失败时优先提示疑似共享网关、网络或公共依赖事故。建立不少于 5～10 个已确认根因的回归案例，人工复核不得在无状态码或外部证据时直接断言 DNS、CORS 或 HTTP 404。
* [ ] **Web Vitals 性能指标接入**：SDK 从纯错误监控扩展至性能监控（FCP、LCP、CLS、TTFB 等），并在后端与前端看板增加性能大盘。
* [ ] **SourceMap 自动化闭环**：提供标准化的 Webpack/Vite 插件或 Node CLI 脚本，支持在业务线 CI/CD 流水线构建完成后，自动将生成的 `.map` 文件连同 `appkey` 和 `version` 上传至监控系统，消除目前手动上传的成本。
* [ ] **企微/钉钉告警集成**：除了邮件告警，支持通过 Webhook 将严重告警推送到团队工作群。

### Phase 3：高级特性与架构升级（长期规划）
* [ ] **会话录制（Session Replay）**：集成 `rrweb`，在发生严重错误时回放用户操作时的页面真实录像，实现像素级的问题复原。
* [ ] **智能降噪与阈值推荐**：基于过去三个月收集的数据模型，利用算法自动建议业务组件的告警阈值，减少「狼来了」式的无效告警。
* [ ] **存储层重构**：正式将日志流水表迁移至 ClickHouse，支持亿级数据的秒级聚合查询。

---

## 六、 Leader 指导意见跟进状态与 AI 演进专项

在之前的内部分享后，Leader 提出了 8 项高价值建议，当前跟进状态如下：

| 序号 | 建议内容 | 当前跟进状态 | 后续演进计划 |
| --- | --- | --- | --- |
| 1 | **添加监控指标（无 SDK 支撑）** | 🟡 部分完成 | SDK 已覆盖核心异常，下半年计划引入 Web Vitals 性能指标（FCP/LCP等）补齐性能监控能力。 |
| 2 | **常见6大类覆盖及自定义补充** | 🟢 已完成 | 已实现 JS、Promise、资源、网络、白屏 5大类自动捕获，并提供 `monitor.captureError` 自定义手动上报 API。 |
| 3 | **微前端项目实验 (PaaS)** | ⚪ 待验证 | SDK 当前为全局单例，理论支持微前端。后续将结合微前端框架（如 qiankun）进行隔离测试。 |
| 4 | **SourceMap 安全与打包问题** | 🟢 已完成 | 已实现后端根据 `Appkey` 物理隔离解析 `.map` 文件。前端生产包彻底移除 SourceMap，解决包体积和泄露风险。 |
| 5 | **AI 类的内容尝试** | 🟡 功能已上线，质量增强待办 | Ask AI 与 AI 巡检日报均已上线；通知链路已完成生产验收。Ask AI 的功能可用性已确认，但诊断准确性尚未形成生产样本回归标准，2026-08-20 网关事故已转化为 P1 可信度增强任务。 |
| 6 | **用户权限、租户与数据权限** | 🟢 已完成 | 以后台 JWT 鉴权为基础，实现了基于 `Appkey` 的多租户数据隔离模型。 |
| 7 | **上报被券商防火墙拦截** | ⚪ 待解决 | 目前依赖外网域名上报。对于严格内网的券商，后续规划「独立域名代理转发」或「私有化部署」方案。 |
| 8 | **使用说明文档** | 🟢 已完成 | 产出了《云监控系统技术方案文档》与《Bug追踪与系统说明》。 |

### 🎯 AI 结合探索方案（低成本高收益）
结合当前系统的数据结构，建议在以下两个方向引入 AI，以极低的开发成本实现业务价值的跃升：
1. **AI 错误诊疗师 (Ask AI)**：已上线。在错误详情页将还原后的报错堆栈与面包屑提交给大模型，生成根因分析和修复建议。2026-08-20 真实网关事故表明，浏览器侧证据只能支持“异常现象与候选原因”，不足以独立确认 DNS、CORS、网关或 404；后续按 Phase 2 的 P1 任务补齐证据分级、跨错误聚合、置信度表达和人工回归评测。
2. **AI 智能日报推送**：已上线并完成生产验收。汇总前一日异常数据生成摘要，并通过 HTML 邮件与 Vanish Markdown 文本发送；真实账号已确认手动与 09:00 自动日报链路。
3. **实时规则告警**：Vanish 触发及 09:00 自动日报连续稳定性两项生产验收均已通过；富消息卡片仍属于后续可选优化，不影响当前里程碑关闭。

---

## 七、 附录：日常开发与维护备忘

### 7.1 服务启动说明
* **后端**：`cd 后端服务 && npm install && npm run dev` (监听 9998 端口)
* **前端后台**：`cd 告警信息可视化网页 && npm install && npm run dev` (监听 5173 端口)
* **SDK构建**：在已有依赖的构建环境执行 `cd 监控sdk源码 && npm.cmd run build`，再执行 `npm.cmd run release:backend`，将版本化 SDK、Loader 和 manifest 写入后端镜像构建目录。

### 7.2 线上数据库访问须知
* 线上已接入 **ProxySQL**，后端代码中已通过 `/* FORCE_MASTER */` 解决主从复制延迟导致的读写不一致问题。
* 请勿在线上服务中直连开发环境的 SQLite 数据库，一切以测试/生产环境配置的 MySQL 库为准。

### 7.3 2026-08-12 SDK 稳定性与共享发布改造
* **配置语义修复**：`resource: 0`、`network: 0` 能按 0% 生效；资源、XHR、Fetch、Promise 开关独立；`dedupeWindow: 300` 按 300 秒处理；配置缓存按 AppKey、客户、项目和环境隔离；全局关闭不会被降级逻辑重新开启。
* **精准降噪**：新增 `ignoreResourceUrls`、`ignoreNetworkUrls`，用于过滤已确认无需处理的固定 URL，避免长期全关资源/网络监控。
* **共享发布**：新增 `monitor-loader.js`、`sdk-manifest.json` 和版本化目录；后端同时提供 `/monitor-sdk` 直连路径和 `/api/monitor-sdk` 正式网关路径，业务组件使用 `https://monitor-api.0033.com/api/monitor-sdk/*`。业务组件完成一次 Loader 迁移后，SDK 后续升级无需逐个组件重新发包。
* **安全边界**：现有 SourceMap 存储保持私有，不与公开 SDK 文件混放；如未来改用文件服务器，应使用独立的公开只读 SDK 目录/Bucket，并配置 HTTPS、CORS、MIME、缓存和 CSP 白名单。
* **当前状态**：SDK 版本已提升至 `1.0.1`，配置/运行时/Loader Mock 12/12 通过，相关 JS 语法与差异检查通过；后端网关别名已部署，Loader 生产地址和龙虎榜2.0业务试点于 2026-08-19 验收通过。
* **2026-08-19 接入复核**：业务组件应从 Loader 回调获取实际 `version` 并用于 `sdkVersion` 上报。配置请求内仍存在历史常量 `version: '1.0.0'`，但当前后端配置接口不读取该字段，所以不会导致 1.0.1 被当成 1.0.0，也不影响采样配置；统一版本变量安排在下次 SDK 发版。现有业务侧时间字符串转换暂时保留，避免在 Loader 迁移中同时引入时区行为变化。
* **2026-08-19 验收结论**：正式页面成功获取共享 Loader 和 manifest，manifest 指向 `1.0.1/monitor.min.js`，控制台显示实际 SDK 版本 `1.0.1`，动态配置成功加载。本次生产试点验收通过；其他业务组件后续完成一次 Loader 迁移后即可共享统一升级能力。

---
> **To 开发者 (AI & User)**：在接下来的开发对话中，请随时参考本文档的 [阶段划分] 与 [技术债] 章节。如果需要开展新特性的编码，请优先完成 Phase 1 中的高优历史遗留问题修复。
