# 云监控系统 - 后续行动与演进追踪计划

> **更新时间**：2026-08-17
> **当前状态**：✅ 阶段一技术债、UI-01~UI-05、Ask AI 与 AI 巡检日报均已上线；**巡检日报邮件、Vanish 正式文本消息、实时规则告警 Vanish 触发及每日 09:00 自动日报连续稳定性均已验收通过**。SDK 1.0.1 配置修复与共享 Loader、管理端 Axios 依赖移除均已完成代码和 Mock 验证，待生产发布验收。

本计划清单用于追踪本系统向「成熟企业级组件」与「AI诊断平台」演进过程中的核心任务。

---

## ✅ 阶段零：近期内部分享筹备（已完成）

- [✔] **【材料准备】编写并完善内部分享 PPT**
  - ✅ 已于 **2026-07-14 晚**完成组内分享，展示样板间成果及 AI 演进方向。leader 正式认可，并同意向券商推进。后续前端在给券商**更新线上云组件**时，可**按需引入**监控 SDK，无需强制全量接入。
- [✔] **【原型开发】完成"Ask AI"功能最小可行产品 (MVP) 供演示**
  - 已完成 (2026-06-25)，现场演示效果良好。

---

## ✅ 阶段一：扫清历史技术债（已全部上线）

> **状态更新（2026-08-04）**：TD-01、TD-02 均已编码完成并部署上线，UI-01~UI-05 已上线验证通过。

### TD-02 【后端服务】重构 SourceMap 解析为"按需异步解析"

- [✔] **已上线**
  - 改动文件：`后端服务/src/routes/errors.js`
  - `/report` 接口移除同步 `await sourcemapService.parseStack()`，`original_stack` 留空占位。
  - `GET /:id` 详情接口新增按需实时解析逻辑，兼容历史已解析数据。

### TD-01 【前端 SDK】修复 Payload 截断静默丢弃风险

- [✔] **已上线**
  - 改动文件：`监控sdk源码/src/core/modules/error-monitor.js`
  - 新增 `safeTruncatePayload()` 函数（两步降级），在 `monitorReport` 调用前执行压缩，确保 Payload ≤ 64KB。

### UI-01 ~ UI-05 管理平台 UI 优化

- [✔] **已全部上线验证**（2026-07-15 开发，后续已验证）
  - UI-01：SourceMap 管理 Appkey 搜索精准匹配
  - UI-02：实例监控详情抽屉支持编辑非关键字段
  - UI-03：数据管理关联指标 ID 改为下拉选择 + 非法提交拦截
  - UI-04：配置管理新增参数说明面板（21个配置键）
  - UI-05：Dashboard 环形图渲染修复（圆弧截断 + 图例重叠）

---

## 🟡 管理端 Axios 依赖移除（代码完成，待生产发布）

- [✔] **统一请求层已迁移至浏览器原生 Fetch**（2026-08-17）
  - 删除管理端 `axios` 直接依赖，页面现有 `request.get/post` 调用无需改动。
  - 保留 `/api` 基础路径、查询参数、JSON 请求、HttpOnly Cookie、10 秒默认超时、SourceMap 上传 60 秒超时、401 登录失效跳转和业务错误提示。
  - `FormData` 上传由浏览器自动生成 multipart boundary，避免手工请求头造成文件上传失败。
- [✔] **本地静态与 Mock 验证完成**
  - `http-client` Mock 单测 5/5 通过，覆盖 GET 查询参数、JSON POST、FormData、HTTP 401 错误结构和超时中止。
  - 新请求层与页面适配层 `node --check` 通过；未启动前后端、未安装依赖、未访问生产服务。
- [ ] **待用户部署与页面验收**
  1. 构建并部署管理端镜像，确认产物中不再包含 Axios。
  2. 验证登录、列表查询/筛选、数据保存/删除、SourceMap 文件上传。
  3. 使用失效会话确认非登录接口 401 会清理登录态并跳转登录页。

---

## 🟢 阶段二：AI 赋能，打造"杀手锏"特性

### ✅ Ask AI 智能错误诊断

- [✔] **已上线**
  - 在「错误日志详情」侧边栏新增 `[AI 智能诊断]` 按钮，提取还原后堆栈 + 面包屑轨迹，调用大模型给出根因分析与修复建议（Markdown 格式输出）。
  - 后端接口：`POST /api/ai/analyze`

### ✅ AI 巡检日报与 Vanish 告警（已上线并完成生产验收）

- [✔] **生产部署完成，日报邮件、Vanish 文本、实时规则告警及 09:00 自动任务均已验收通过**（2026-08-12）

**功能概述**：每天 09:00 上海时间自动执行，汇总前一日错误数据，调用大模型生成结构化 JSON 摘要；按页面配置分别发送富文本 HTML 邮件和 Vanish 文本摘要。实时规则告警也可在每条规则中独立启用 Vanish 账号通知。

**Vanish 接入状态（2026-08-12）**：
  - 扶摇 `云监控 Vanish 告警推送 v1.1` 已发布正式集群；云监控调用方代码已接入。
  - 日报通知配置支持邮件/Vanish 独立开关和独立收件账号；告警规则支持单独配置 Vanish 账号。
  - 正式 URL 与 AK 仅从 `FUYAO_VANISH_ALERT_URL`、`VANISH_AK` 环境变量读取，页面和数据库不保存凭证。
  - Vanish 发送不自动重试，且必须同时校验 HTTP、`status_code=0`、`succeed=true`、`downstreamStatus=0`。
  - 生产联调先后识别并解决 AK 权限与 Vanish 下游配置问题；最终真实账号已收到 AI 巡检日报，正式文本链路验证通过。
  - 当前 Vanish 内容为 Markdown 文本摘要；富消息卡片、图片或文件不是本期已验收范围。
  - 实时规则告警 Vanish 触发，以及每日 09:00 自动日报连续稳定性两项均已完成生产验收。

**涉及改动文件**：
  - `后端服务/src/db/models/dailyReport.js` **[新增]** — daily_reports 数据模型
  - `后端服务/src/services/aiDailyReport.js` **[新增]** — 核心服务（聚合 + AI + 邮件/Vanish）
  - `后端服务/src/routes/aiDailyReport.js` **[新增]** — 日报与通知配置 REST 接口
  - `后端服务/src/services/email.js` **[修改]** — 新增 `sendDailyReportEmail()` 方法
  - `后端服务/src/services/vanish.js` **[新增]** — 扶摇 Vanish 客户端、日报/告警消息渲染与业务成功校验
  - `后端服务/src/db/models/config.js` **[修改]** — 新增 `getSystemConfig/setSystemConfig()`
  - `后端服务/src/db/schema.sql` **[修改]** — SQLite 结构参考（本项目无需本地执行）
  - `后端服务/src/db/schema_mysql.sql` **[修改]** — MySQL 结构参考（应用不会自动执行）
  - `后端服务/src/app.js` **[修改]** — 注册路由 + 启动 Cron 定时器
  - `告警信息可视化网页/src/views/AiDailyReport.vue` **[新增]** — 日报管理与双渠道通知配置页面
  - `告警信息可视化网页/src/views/DataManagement.vue` **[修改]** — 告警规则 Vanish 开关与账号配置
  - `告警信息可视化网页/src/views/InstanceMonitor.vue` **[修改]** — 实例详情中查看/编辑 Vanish 告警配置
  - `告警信息可视化网页/src/router/index.js` **[修改]** — 新增路由
  - `告警信息可视化网页/src/components/MainLayout.vue` **[修改]** — 侧边栏新增菜单

**部署状态**：生产 MySQL 结构变更由用户人工执行，应用不执行自动迁移；云监控后端与前端已发布正式环境。

> **正式环境硬约束**：生产 MySQL 只能由用户通过数据库工单或人工执行 DDL，应用账号没有建表/改表权限。代码不得自动迁移；已知表存在时，直接在交付回复中给出最小 `ALTER TABLE` 命令，不额外创建迁移 SQL 文件。

生产库已有 `daily_reports`，本期由用户人工补充 `vanish_recipients`、`vanish_sent` 两列；后续如再改表，仍只在交付回复中提供最小 DDL。

**本项目验证边界**：本地不启动前后端、不安装依赖补环境，只执行 `node --check` 等语法检查和使用 Mock 的简单单元自测。生产部署、数据库执行、页面操作、正式邮件及 Vanish 消息接收由用户完成并确认；本地检查通过不等于功能已上线或生产验收通过。

**前台新增页面**：`/console/ai-daily-report`
  - 历史日报列表 + 手动触发生成 + 邮件/Vanish 通知配置
  - 详情抽屉：数据统计 Tab（ECharts 饼图 + 条形图）+ AI 分析 Tab（结构化洞察卡片）

**验收结论（2026-08-12）**：
  1. 实时规则告警 Vanish 触发验收通过。
  2. 每日 09:00 自动日报连续稳定性验收通过。
  3. 本里程碑关闭。Vanish 富消息卡片属于后续可选优化，不阻塞阶段三优先级评审。

---

### 🟡 SDK 1.0.1 配置修复与共享 Loader（代码完成，待生产发布）

- [✔] **SDK 配置与运行时修复完成**（2026-08-12）
  - `resource: 0`、`network: 0` 能真正按 0% 采样，网络采样覆盖 XHR 错误、超时、中止和 Fetch 等子类型。
  - 资源、XHR、Fetch、Promise 开关独立生效；`dedupeWindow: 300` 正确转换为 300 秒。
  - 配置缓存按 AppKey、客户、项目和环境隔离；全局关闭不会被降级逻辑重新开启。
  - SDK 版本提升至 `1.0.1`，页面可通过 `window.DynamicWebMonitor.version` 核对实际加载版本。

- [✔] **精准 URL 降噪完成**（2026-08-12）
  - 新增 `ignoreResourceUrls`、`ignoreNetworkUrls`，用于忽略已经确认无须处理的固定资源或接口。
  - 长期策略为“保留小比例采样 + 精准忽略已知噪声”，不以永久关闭全部 network/resource 代替监控治理。

- [✔] **共享 Loader 与版本化发布代码完成**（2026-08-12）
  - 新增稳定 Loader、`sdk-manifest.json` 和 `{version}/monitor.min.js` 版本目录。
  - 云监控后端新增 `/monitor-sdk` 静态托管入口；Loader/manifest 不缓存，版本文件长期缓存；保留旧版本文件时可通过 manifest 回退。
  - 业务组件仍需进行一次 Loader 迁移；完成后，SDK 后续升级只需发布新版本并更新 manifest，无需逐个组件重新发包。
  - 当前 SourceMap 存储继续保持私有；未来如使用文件服务器，应为 SDK 建立独立公开只读目录/Bucket，不能直接公开 SourceMap。

- [✔] **本地验证完成**
  - 配置、运行时与 Loader Mock 共 12/12 通过；相关 JS `node --check`、JSON 解析及 `git diff --check` 通过。
  - 未启动前后端、未安装依赖、未访问生产数据库或真实外部服务。

- [ ] **待用户生产发布与验收**
  1. 在已有依赖的构建环境执行 `cd 监控sdk源码`、`npm.cmd run build`、`npm.cmd run release:backend`。
  2. 确认 SDK 产物进入 `后端服务/public/monitor-sdk` 后，再构建并部署后端镜像。
  3. 选择一个业务组件完成 Loader 试点，确认浏览器 CORS/CSP、`DynamicWebMonitor.version === '1.0.1'`、采样和精准过滤符合预期。

---

## 🔵 阶段三：深耕业务场景与功能扩张（中期演进）

- [ ] **【监控能力】新增 Web Vitals 性能监控大盘**
  - **任务**：扩展 SDK，通过浏览器 `PerformanceObserver` API 采集 FCP、LCP、CLS、TTFB 等性能核心指标。
  - **要求**：在管理后台单独建立「性能数据面板」，可监控组件在不同客户网络下的首屏加载速度。

- [ ] **【工程化】SourceMap 自动化闭环**
  - **任务**：编写 `b2b-monitor-webpack-plugin`。
  - **要求**：业务线执行 `npm run build` 后，插件自动将其产生的 `.map` 文件通过 API 上传至监控系统对应 AppKey 目录下，实现研发零干预。

- [ ] **【安全合规】券商私有化部署 / 代理网关**
  - **任务**：针对强内网券商客户，出具网络连通性解决方案。
  - **要求**：提供 Nginx 反向代理配置 Demo，或将当前 Node后端 + Vue前端 打包为私有化 Docker 镜像，支持在客户内网一键部署。

- [ ] **【告警渠道】企微 / 钉钉 Webhook 告警**
  - **任务**：在现有邮件告警之外，支持通过 Webhook 将严重告警（L1 级）推送到团队工作群。
  - **要求**：告警规则配置页新增 Webhook URL 字段，消息格式适配企微/钉钉 Markdown card。

---

## ⚪ 阶段四：海量数据架构演进（长期储备）

- [ ] **【存储层】MySQL 容量与查询性能观察 (TD-03，暂缓改造)**
  - **当前基线（2026-08-12）**：两张主要数据表合计约 1.97MB，完全没有容量压力，不需要清表、加库或引入 ClickHouse。
  - **触发条件**：持续记录表容量、日新增量、慢查询和索引命中情况；仅当真实容量或查询性能达到明确阈值后，再评估归档、索引优化或 ClickHouse。

- [ ] **【会话录制】Session Replay（rrweb）**
  - **任务**：集成 `rrweb`，在发生严重错误时回放用户操作时的页面真实录像，实现像素级的问题复原。

---

## 📌 里程碑时间线

| 时间 | 里程碑 |
|------|--------|
| 2026-06-25 | Ask AI MVP 完成 |
| 2026-07-14 | 组内分享完成，leader 认可 |
| 2026-07-15 | TD-01/TD-02/UI-01~05 编码完成 |
| 2026-08-04 | TD-01/TD-02/UI/Ask AI 全部上线；AI 巡检日报开发完成 |
| 2026-08-07 | AI 日报与实时告警完成 Vanish 渠道代码接入；待正式环境配置和验收 |
| 2026-08-11 | AI 巡检日报部署上线；邮件与 Vanish 正式文本消息由真实账号实收确认 |
| 2026-08-12 | 实时规则告警 Vanish 触发与 09:00 自动日报连续稳定性验收通过；SDK 1.0.1 配置修复、精准 URL 过滤及共享 Loader 完成代码与 Mock 验证，待生产发布 |
| 2026-08-17 | 管理端统一请求层迁移至原生 Fetch，移除 Axios 直接依赖并完成 5/5 Mock 验证；待管理端部署及页面验收 |
| 待定 | 阶段三功能开始排期 |
