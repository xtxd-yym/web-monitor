# 云监控系统 - 后续行动与演进追踪计划

> **更新时间**：2026-07-14
> **当前状态**：内部分享完成，leader 认可，即将向大范围业务线/券商客户铺开（渐进式接入）。

本计划清单用于追踪本系统向「成熟企业级组件」与「AI诊断平台」演进过程中的核心任务。

---

## ✅ 阶段零：近期内部分享筹备（已完成）

- [✔] **【材料准备】编写并完善内部分享 PPT**
  - 已完成内部汇报，展示样板间成果及 AI 演进方向。leader 认可，后续准备前端给券商出云组件时渐进式接入。
- [✔] **【原型开发】完成"Ask AI"功能最小可行产品 (MVP) 供演示**
  - 已完成 (2026-06-25)，现场演示效果良好。

---

## 🟢 阶段一：扫清历史技术债（高优先级，铺开前必做）

在向券商真实业务大面积接入前，系统必须具备绝对的抗压与容错能力。

> **影响范围说明**：SDK 上报使用 `fetch` 异步发出，不阻塞宿主页面渲染与交互，**不影响券商用户的页面体验**。但以下两个问题会导致监控系统自身出现数据丢失或服务降级，必须在铺开前修复。

---

### 第一步：TD-02 【后端服务】重构 SourceMap 解析为"按需异步解析"

- [ ] **实施完成**

**问题根因**：`errors.js` 的 `/report` 接口在 `for` 循环中对每条错误同步 `await sourcemapService.parseStack()`（L98），每次解析涉及磁盘 I/O（读取 `.map` 文件）+ CPU 运算（位置映射），高并发时严重阻塞 Node.js Event Loop，上报接口响应时间从毫秒级飙升至秒级，请求排队积压，最终导致监控数据大面积丢失。

**宿主页面影响**：❌ 不受影响（SDK `fetch` 异步上报，不等响应）。影响范围仅限监控系统后端本身。

**实现方案（两步改造）**：

**Step 1 - 上报接口只管快速落库，完全移除 SourceMap 解析（改动：`errors.js` `/report` 路由）**

```js
// 删除以下代码块（errors.js L94~L102）：
// let originalStack = '';
// if (item.stack) {
//     try {
//         originalStack = await sourcemapService.parseStack(item.stack, appkey);
//     } catch (e) { ... }
// }

// extra_data 中 original_stack 留空占位，待查询时按需填充：
extra_data: JSON.stringify({
    ...(item.data || {}),
    original_stack: '',   // 占位，查询时实时翻译
    userAgent: item.userAgent || '',
    pageUrl: item.url || ''
}),
```

**Step 2 - 在"查询错误详情"接口按需实时解析（改动：`errors.js` `GET /:id` 路由）**

```js
router.get('/:id', async (req, res) => {
    const error = await errorModel.findById(parseInt(id));

    // 🆕 按需解析：仅在查看详情时执行，不在上报时执行
    let originalStack = '';
    const appkey = error.appkey || '';
    if (error.stack && appkey) {
        try {
            originalStack = await sourcemapService.parseStack(error.stack, appkey);
        } catch (e) {
            console.warn('[SourceMap] 按需解析失败，返回原始堆栈:', e.message);
            originalStack = error.stack; // 降级返回混淆堆栈
        }
    }

    res.json({
        success: true,
        data: { ...error, original_stack: originalStack, breadcrumbs }
    });
});
```

**验收**：JMeter 对 `/api/errors/report` 进行 500 QPS 压测，P99 响应时间 < 50ms，无 Event Loop Lag 告警。

---

### 第二步：TD-01 【前端 SDK】修复 Payload 截断静默丢弃风险

- [ ] **实施完成**

**问题根因**：`error-monitor.js` 的 `reportErrors` 将含 `breadcrumbs` 面包屑的 `list` 直接序列化上报，极限情况（200 条面包屑 + 深层堆栈）Payload 可能超过 64KB，被浏览器或网关静默丢弃，**且没有任何告警**，关键错误指纹就此丢失，对接入方产生信任危机。

**宿主页面影响**：❌ 不受影响。但会导致监控系统漏报。

**实现方案（改动：`error-monitor.js` 中 `reportErrors` 函数，在 `monitorReport` 调用前插入）**

```js
function safeTruncatePayload(reportData, limitBytes = 64 * 1024) {
    let json = JSON.stringify(reportData);
    if (json.length <= limitBytes) return reportData; // 无需截断

    // --- 第一步降级：面包屑缩减到最近 5 条 ---
    console.warn('[Monitor][TD-01] Payload 超限，执行第一步降级：截断面包屑');
    const step1 = {
        ...reportData,
        list: reportData.list.map(item => ({
            ...item,
            breadcrumbs: (item.breadcrumbs || []).slice(-5)
        }))
    };
    json = JSON.stringify(step1);
    if (json.length <= limitBytes) return step1;

    // --- 第二步降级：清空面包屑 + 截断堆栈只保留前 5 行 ---
    // fingerprint / type / message 等核心字段保持不变，错误身份信息不丢
    console.warn('[Monitor][TD-01] Payload 仍超限，执行第二步降级：截断堆栈');
    return {
        ...step1,
        list: step1.list.map(item => ({
            ...item,
            breadcrumbs: [],
            stack: (item.stack || '').split('\n').slice(0, 5).join('\n') + '\n... (stack truncated)'
        }))
    };
}

// 调用处（reportErrors 函数中，monitorReport 调用前）：
const safeReportData = safeTruncatePayload(reportData);
response = await monitor.options.monitorReport(safeReportData);
```

**验收**：构造一条带 200 条面包屑的极限错误并触发上报，Network 面板中请求 Payload ≤ 64KB，后端能正确入库并保留 `fingerprint`、`type`、`message` 等核心字段。

---

## 🟡 阶段二：AI 赋能，打造"杀手锏"特性（展示期与亮点）

迎合公司内部对 AI 应用的推崇，以极低成本集成大模型能力，提升系统的核心业务价值。

- [ ] **【大模型接入】增加"Ask AI"智能错误诊断**
  - **任务**：在控制台"错误日志详情"侧边栏新增 `[AI 智能诊断]` 按钮。
  - **要求**：点击时，提取 **还原后的报错栈** + **报错前 5 步的 Ajax 请求/点击轨迹**，拼接为系统 Prompt 调用公司大模型接口。
  - **目标产出**：AI 需直接指出业务代码中哪个变量引发了问题（如 `tagIndexes 为空`），并给出修复代码段（如 `添加 Optional Chaining`）。
  
- [ ] **【大模型接入】AI 巡检日报**
  - **任务**：编写定时脚本（Node.js Cron），每天上午 9 点查库汇总前一日数据。
  - **要求**：调用 AI 接口总结数据（"X组件错误突增，问题集中在..."），通过现有邮件服务发送给负责人。

---

## 🔵 阶段三：深耕业务场景与功能扩张（中期演进）

- [ ] **【监控能力】新增 Web Vitals 性能监控大盘**
  - **任务**：扩展 SDK，通过浏览器 `PerformanceObserver` API 采集 FCP、LCP、CLS、TTFB 等性能核心指标。
  - **要求**：在管理后台单独建立「性能数据面板」，可监控组件在不同客户网络下的首屏加载速度。

- [ ] **【工程化】SourceMap 自动化闭环**
  - **任务**：编写 `b2b-monitor-webpack-plugin`。
  - **要求**：业务线执行 `npm run build` 后，插件自动将其产生的 `.map` 文件通过 API 上传至监控系统对应 AppKey 目录下，实现研发零干预。

- [ ] **【安全合规】券商私有化部署 / 代理网关**
  - **任务**：针对强内网券商客户，出具网络连通性解决方案。
  - **要求**：提供 Nginx 反向代理配置 Demo，或将当前 Node后端 + Vue前端 打包为私有化 Docker 镜像，支持在客户内网一键部署。

---

## ⚪ 阶段四：海量数据架构演进（长期储备）

- [ ] **【存储层】MySQL 数据膨胀优化 (TD-03)**
  - **任务**：引入 ClickHouse。
  - **要求**：将高频写入的 `error_logs` 与 `user_breadcrumbs` 流水表迁移至 ClickHouse，支持百亿级数据秒级多维筛选。MySQL 退居二线，仅存储租户配置、鉴权与告警规则等强一致性元数据。
